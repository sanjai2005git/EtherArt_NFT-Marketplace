apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: etherart-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: etherart.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: etherart-service
                port:
                  number: 80
